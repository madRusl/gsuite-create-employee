* setup [GAM](https://github.com/jay0lee/GAM)
* fill-in `.config` with creds and tokens
* modify `mail/footer.sh` and `mail/header.sh` and according to your needs
* change gsuite configs in `employee.sh`